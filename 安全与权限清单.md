# GitHub Token 权限清单（探测记录）

> 日期：2026-08-12 ｜ 仓库：`liyingcake/Kardashev-The-Exponent`（public）
> ⚠️ 本文件**不包含 Token 明文**。Token 仅存于本机 Windows 凭据管理器（条目 `git:https://github.com`），供 git 免密使用。

## 1. Token 类型

- 前缀 `github_pat_` → **Fine-grained PAT（细粒度令牌）**
- 细粒度令牌的权限不公开可查，本清单基于**实测操作**记录

## 2. 实测验证结果

| 操作 | 结果 | 结论 |
|---|---|---|
| `GET /user` | 成功，liyingcake（id 33079394，type User） | ✅ 身份读取可用 |
| `GET /user/orgs` | 空 | ⚠️ 无组织（或未授权组织读取） |
| `GET /repos/liyingcake/...` | 成功（public，default main） | ✅ 仓库读取可用 |
| `GET /repos/.../branches` | 成功 | ✅ 分支读取可用 |
| `GET /repos/.../contents` | 成功 | ✅ 内容读取可用 |
| **`git push origin main`** | **成功**（2b93dde→270ccf7） | ✅ **写入推送可用** |
| `git fetch` / `ls-remote` | 成功 | ✅ 拉取可用 |
| Windows 凭据管理器 | `git:https://github.com` 已持久化 | ✅ 后续免密 |

## 3. 推断的有效权限（基于实测）

- **Contents：读写**（推送成功即证明）
- **Metadata：读取**（GitHub 对细粒度令牌强制授予，仓库可被发现）
- **仓库访问级别**：public 仓库 + 授权 token → 读 + 写（当前 owner 身份）
- 未实测：Issues / PR / Actions / Secrets / 管理类操作（本项目暂不需要，不做）

## 4. 安全处置

- Token 已按用户要求"拿好"：仅存在于 Windows 凭据管理器，**未写入任何文件、未进入 git 仓库、未出现在任何 commit 中**。
- 推送过程使用 `git credential approve` 写入凭据管理器（会话级 → 持久化，已验证）。
- 处置脚本 `verify_token.ps1` 已删除（从环境变量读取，不落盘）。
- **风险提示**：Token 已在本会话明文出现；若此会话记录泄露，建议在 GitHub 撤销该 token 并重新生成（仓库设置中该 token 权限可随时调整）。
- 撤销路径：GitHub → Settings → Developer settings → Fine-grained tokens → 对应 token → Revoke。

## 5. 使用范围

- 本 token 仅用于：`git push`/`fetch` 到 `liyingcake` 名下的仓库（当前仅 Kardashev-The-Exponent）。
- 若未来新增仓库需推送，此 token 若已授权可复用；若未授权需重新授权或新建。

---
*记录来源：Token 实测探测（来自聪明的大赢鲸的思考与认知，基于实际 API/push 行为）。*
